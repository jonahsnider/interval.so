{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "pnpm build --filter=@interval.so/web",
	"cleanUrls": true,
	"framework": "nextjs",
	"installCommand": "pnpm install --frozen-lockfile",
	"outputDirectory": "apps/web/.next"
}
