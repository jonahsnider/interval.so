{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "yarn run build --filter=@interval.so/web",
	"cleanUrls": true,
	"framework": "nextjs",
	"installCommand": "yarn install --immutable",
	"outputDirectory": "apps/web/.next"
}
